- [ ] remove `JobStatus::Started`
  + originally for 2-phase design (manager gets, sends request to worker pool, marks as started)
  + now it's just queued -> running as workers dequeue directly (_no 'job maanger'_)
- [ ] add `created_at` to `JobState` and order by it to ensure workers deque FIFO
