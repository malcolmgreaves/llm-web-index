# core-ltx

Core LLM Text generation library & CLI. Contains:

- `llm.txt` generation logic
- updating an `llm.txt` file
- retry logic + prompts for fixing `llm.txt` files
- CLI for one-off `llm.txt` file generation
