# Test Environment Configuration
#
# This file contains environment variables for running tests.
# Copy this to .env when running tests, or source it directly.

# Test Database Configuration
TEST_DATABASE_URL=postgres://ltx_test_user:ltx_test_password@localhost:5433/ltx_test_db
DATABASE_URL=postgres://ltx_test_user:ltx_test_password@localhost:5433/ltx_test_db

# Logging Configuration
RUST_LOG=debug
RUST_BACKTRACE=1

# Test-specific settings
# No real API keys needed for tests (we use mocks)
# OPENAI_API_KEY is not required for unit tests with mock providers
